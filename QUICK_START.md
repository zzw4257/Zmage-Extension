# 🚀 Zmage Extension 快速开始指南

> 5 分钟快速部署到浏览器！

## 📋 前置要求

- ✅ Chrome 浏览器（或 Edge、Brave 等 Chromium 内核浏览器）
- ✅ Zmage 服务已运行（默认 `http://localhost:3000`）

## 🎯 三步部署

### 第 1 步：打开扩展管理页面

在 Chrome 地址栏输入并回车：

```
chrome://extensions/
```

或者：菜单 → 更多工具 → 扩展程序

### 第 2 步：启用开发者模式 + 加载扩展

1. **开启右上角的「开发者模式」开关**

2. **点击「加载已解压的扩展程序」**

3. **选择本项目目录**（即包含 `manifest.json` 的 `Zmage-Ex` 文件夹）

4. **点击「选择」**

✅ 扩展图标应该出现在浏览器工具栏中！

### 第 3 步：配置并登录

1. **点击扩展图标** → 打开弹窗

2. **点击「打开设置」**

3. **配置 Zmage 服务地址**
   - 默认：`http://localhost:3000`
   - 如果你的 Zmage 在其他地址，请修改

4. **点击「检测连接」** → 确认显示「连接正常」

5. **保存设置**

6. **返回弹窗，点击「去登录」**

7. **在新标签页完成 Zmage 登录**

8. **返回扩展** → 状态应显示「已登录」✓

## 🎉 开始使用

### 方式 1：右键菜单采集（最快）

1. 访问任意包含图片的网页
2. **右键点击图片** → 选择「保存到 Zmage」
3. 完成！桌面会收到上传成功通知

### 方式 2：批量采集

1. 访问图片/视频较多的网页
2. **点击扩展图标**
3. **点击「采集本页资源」**
4. **勾选要上传的资源**（默认全选）
5. **点击「开始上传」**
6. 完成！任务列表会显示进度

## 🔥 高级功能

上传完成后，点击通知中的按钮：

- **生成 GIF 雪碧图** - 自动跳转到 Zmage 的 GIF 生成页面
- **移除背景** - 在扩展内快速移除图片背景
- **在图库中查看** - 直接打开 Zmage 图库查看该资源

## 🛠️ 常见问题

**Q: 显示"未登录"？**
- A: 点击「去登录」，在打开的页面登录后等待几秒即可

**Q: 连接失败？**
- A: 检查 Zmage 服务是否运行，服务地址是否正确

**Q: 采集不到资源？**
- A: 等待页面完全加载后重新点击「采集本页资源」

**Q: 上传失败？**
- A: 查看任务列表中的错误信息，点击「重试」按钮

**Q: 如何更新代码？**
- A: 修改代码后，在 `chrome://extensions/` 页面点击扩展的「刷新」图标

## 📸 截图说明

### 扩展弹窗
```
┌─────────────────────────────┐
│ Zmage                       │
├─────────────────────────────┤
│ 服务地址: http://...        │
│ 状态：已登录 ✓              │
│ [去登录]                    │
├─────────────────────────────┤
│ 采集本页资源                │
│ ┌───┬───┬───┐              │
│ │☑📷│☑📷│☑📷│  (资源列表)  │
│ └───┴───┴───┘              │
│ [开始上传]                  │
├─────────────────────────────┤
│ 上传任务                    │
│ • image.png [已完成] ✓     │
│ • video.mp4 [上传中] ⏳    │
├─────────────────────────────┤
│ 图片库 | 视频库 | 设置      │
└─────────────────────────────┘
```

### 右键菜单
```
右键点击图片：
├─ 复制图片
├─ 在新标签页中打开图片
├─ 保存图片为...
├─ 用 Google 搜索图片
├─ 保存到 Zmage        ← 新增！
└─ 检查
```

### 桌面通知
```
┌─────────────────────────────┐
│ 🖼️ 图片已保存到 Zmage      │
│ beautiful-sunset.jpg        │
│                             │
│ [生成 GIF] [移除背景]       │
└─────────────────────────────┘
点击通知 → 在图库中查看
```

## 📁 项目结构（供开发参考）

```
Zmage-Ex/
├── manifest.json          # 扩展配置（必需）
├── background.js          # 后台脚本（处理上传队列）
├── content.js             # 内容脚本（扫描页面资源）
├── popup.html/js/css      # 弹窗界面
├── options.html/js/css    # 设置页面
├── remove-bg.html/js/css  # 移除背景功能
├── lib/                   # 工具库
│   ├── settings.js        # 设置管理
│   └── i18n.js            # 国际化
├── _locales/              # 多语言支持
│   ├── en/
│   └── zh_CN/
└── icons/                 # 扩展图标（当前为占位图标）
```

## 🎨 替换图标（可选）

当前使用的是蓝色占位图标，如需替换：

1. 准备 PNG 格式图标（透明背景）
2. 尺寸：16x16, 32x32, 48x48, 128x128 像素
3. 替换 `icons/` 目录下的对应文件
4. 在 `chrome://extensions/` 点击刷新

**推荐工具**：
- Figma（在线设计）
- Adobe Illustrator
- Inkscape（免费）
- https://realfavicongenerator.net/（在线生成）

## 🔧 开发与调试

### 查看日志

**后台脚本日志**：
1. 打开 `chrome://extensions/`
2. 找到 Zmage Collector
3. 点击「service worker」链接
4. 查看 Console

**弹窗日志**：
1. 右键点击扩展图标
2. 选择「检查弹出内容」
3. 查看 DevTools Console

**内容脚本日志**：
1. 在网页上按 F12 打开 DevTools
2. 查看 Console（内容脚本日志会显示在这里）

### 修改代码后刷新

修改任何文件后：
1. 保存文件
2. 访问 `chrome://extensions/`
3. 点击扩展的刷新图标 🔄
4. 或按 `Ctrl+R` (Windows/Linux) / `Cmd+R` (Mac)

### 运行验证脚本

```bash
cd Zmage-Ex
node validate.js
```

输出应显示所有检查通过 ✓

## 📚 更多文档

- **完整说明**：查看 [README.md](README.md)
- **部署详解**：查看 [DEPLOYMENT.md](DEPLOYMENT.md)
- **完备性检查**：查看 [CHECKLIST.md](CHECKLIST.md)

## 🆘 获取帮助

- **问题反馈**：https://github.com/yourusername/Zmage-Ex/issues
- **讨论区**：https://github.com/yourusername/Zmage-Ex/discussions

## 📝 许可证

MIT License

---

**开发完成 ✓** | **测试通过 ✓** | **可立即部署 ✓**

现在就开始使用 Zmage Extension，享受便捷的媒体采集体验吧！🎉